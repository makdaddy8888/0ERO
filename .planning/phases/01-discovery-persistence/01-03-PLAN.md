---
phase: 01-discovery-persistence
plan: 03
type: execute
depends_on: ["01-02"]
files_modified:
  - src/app/import/page.tsx
  - src/app/import/folder-import-form.tsx
  - src/app/import/import-gate.tsx
  - src/lib/discovery/user-discovery.ts
domain: next-js
---

<objective>
Block `/import` until discovery setup is complete and wire import gap analysis to SQLite-backed profile.

Purpose: Enforce v0.2 completeness gate — users must confirm institutions before importing documents.
Output: Server-side gate on import page, blocked-state UI with link to Setup, import form using server profile.
</objective>

<execution_context>
./.cursor/get-shit-done/workflows/execute-plan.md
./.cursor/get-shit-done/templates/summary.md
./.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-discovery-persistence/01-02-SUMMARY.md
@src/lib/discovery/user-discovery.ts
@src/lib/discovery/discovery-repository.ts
@src/app/import/page.tsx
@src/app/import/folder-import-form.tsx
@docs/BMAD-DISCOVERY.md

**Established patterns:** Server Components for pages; client form for file picker
**Gate rule (Phase 1):** Use existing `isDiscoveryComplete(profile)` — true when ≥1 institution or product confirmed. Full BMAD 85% score gate is Phase 2.

**Depends on:** Plans 01-01 and 01-02 (SQLite profile available via repository/actions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-side import gate and blocked UI</name>
  <files>src/app/import/page.tsx, src/app/import/import-gate.tsx</files>
  <action>
Convert `src/app/import/page.tsx` to an async Server Component:

- Import `loadDiscoveryProfile` and `isDiscoveryComplete` from `discovery-repository` (server)
- If incomplete: render new `ImportGate` component instead of `FolderImportForm`
- If complete: render `FolderImportForm` passing serialized profile as prop

Create `src/app/import/import-gate.tsx` (can be Server Component):

- Heading: "Complete setup before importing"
- Explain: confirm banks/brokers/cards in Setup first
- Primary CTA link to `/setup`
- Secondary link to home
- Match existing dark agentic theme (`agent-card`, `agent-btn`)

Do NOT redirect with `redirect()` — show inline blocked state so user understands why (better UX than silent redirect).

Phase 1 gate uses `isDiscoveryComplete` only — do NOT implement BMAD completeness score yet.
  </action>
  <verify>`npm run build` passes; fresh DB (no setup) would show gate when page renders</verify>
  <done>Incomplete profile shows gate UI; complete profile shows import form</done>
</task>

<task type="auto">
  <name>Task 2: Pass server profile to FolderImportForm</name>
  <files>src/app/import/folder-import-form.tsx, src/lib/discovery/user-discovery.ts</files>
  <action>
Update `FolderImportForm`:

- Add required prop `profile: UserDiscoveryProfile` (passed from server page when gate passes)
- Remove `loadDiscoveryProfile()` call inside `handleFiles` — use prop instead
- Keep client-side file processing unchanged (`processImportFolder(files, profile)`)

Ensure `UserDiscoveryProfile` type is imported from `user-discovery.ts` (shared type).

Add brief comment on page.tsx: profile comes from SQLite via server load — not localStorage.

Do NOT persist import results to SQLite in this plan (Phase 3 scope).
  </action>
  <verify>`npm run build` passes; `npm test` passes; grep shows no `loadDiscoveryProfile` in folder-import-form</verify>
  <done>Import uses server-loaded profile for gap analysis; no localStorage reads on import path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Import completeness gate — blocked when setup empty, open when setup saved</what-built>
  <how-to-verify>
    1. Run: `npm run dev:clean`
    2. Delete `./data/ledger.db` if it exists (fresh state)
    3. Visit: http://127.0.0.1:3000/import — confirm gate message and Setup link (no file picker)
    4. Visit: http://127.0.0.1:3000/setup — add at least one bank/product, save
    5. Visit: http://127.0.0.1:3000/import — confirm folder import form appears
    6. Run a test import — gap analysis still references your confirmed institutions
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `npm run privacy:audit` passes
- [ ] Human verify checkpoint approved
- [ ] `/import` blocked without setup; accessible after setup
</verification>

<success_criteria>

- All tasks completed
- Import gate enforced server-side
- Import gap analysis uses SQLite-backed profile
- Phase 1 complete — ready for Phase 2 (question wizard)
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-persistence/01-03-SUMMARY.md` per summary template.
Update `.planning/STATE.md` — Phase 1 complete, Phase 2 ready to plan.
</output>
