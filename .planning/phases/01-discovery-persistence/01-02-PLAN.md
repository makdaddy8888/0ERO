---
phase: 01-discovery-persistence
plan: 02
type: execute
depends_on: ["01-01"]
files_modified:
  - src/lib/discovery/user-discovery.ts
  - src/lib/discovery/migrate-local-storage.ts
  - src/app/setup/actions.ts
  - src/app/setup/setup-institutions-form.tsx
  - src/app/setup/page.tsx
domain: next-js
---

<objective>
Connect the Setup UI to SQLite via Server Actions and migrate existing localStorage profiles once.

Purpose: Users keep their current setup flow but data persists in `./data/ledger.db` per privacy docs.
Output: Server Actions for load/save, one-time client migration, Setup form reading/writing SQLite.
</objective>

<execution_context>
./.cursor/get-shit-done/workflows/execute-plan.md
./.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-discovery-persistence/01-01-SUMMARY.md
@src/lib/discovery/user-discovery.ts
@src/lib/discovery/discovery-repository.ts
@src/app/setup/setup-institutions-form.tsx

**Tech stack available:** Server Actions (Next.js 15), existing client Setup form
**Established patterns:** `"use client"` forms; `saveDiscoveryProfile` currently sync localStorage
**Constraining decisions:**
- One-time migration: read localStorage once, POST to server, clear localStorage on success
- Keep `isDiscoveryComplete()` as pure function in `user-discovery.ts` for shared client/server logic

**Depends on:** Plan 01-01 repository and migrations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server Actions and localStorage migration helper</name>
  <files>src/app/setup/actions.ts, src/lib/discovery/migrate-local-storage.ts, src/lib/discovery/user-discovery.ts</files>
  <action>
Create `src/app/setup/actions.ts` with `"use server"`:

- `getSetupProfileAction(): Promise&lt;UserDiscoveryProfile | null&gt;` — calls repository `loadDiscoveryProfile`
- `saveSetupProfileAction(profile: UserDiscoveryProfile): Promise&lt;{ ok: true }&gt;` — validates shape (arrays present, FY string), calls repository save

Create `src/lib/discovery/migrate-local-storage.ts` (client-safe):

- `migrateLocalStorageIfNeeded(saveAction): Promise&lt;UserDiscoveryProfile | null&gt;`
- If `localStorage` key `0ero-discovery-profile` exists: call save action, on success remove key and return saved profile
- If no localStorage data: return null (caller loads from server)

Refactor `user-discovery.ts`:
- Keep types, `getDefaultFinancialYear()`, `isDiscoveryComplete()` pure function
- Mark `loadDiscoveryProfile` / `saveDiscoveryProfile` / `clearDiscoveryProfile` as `@deprecated` with comment pointing to Server Actions — keep temporarily for migration helper only
- Do NOT delete localStorage functions until migration helper uses them

Validate profile server-side: reject if `confirmedInstitutionIds` is not an array or FY is empty.
  </action>
  <verify>`npm run build` passes; server actions file has `"use server"` directive</verify>
  <done>Server Actions exist; migration helper reads localStorage once and persists to SQLite</done>
</task>

<task type="auto">
  <name>Task 2: Update Setup form to use Server Actions</name>
  <files>src/app/setup/setup-institutions-form.tsx, src/app/setup/page.tsx</files>
  <action>
Update `setup-institutions-form.tsx`:

On mount (`useEffect`):
1. Call `migrateLocalStorageIfNeeded(saveSetupProfileAction)`
2. Then call `getSetupProfileAction()` if migration returned null
3. Hydrate selected products/institutions from loaded profile (same logic as today)

On save (existing save handler):
- Call `saveSetupProfileAction(profile)` instead of `saveDiscoveryProfile`
- On success show saved state; on error show inline error message

Remove direct `loadDiscoveryProfile` / `saveDiscoveryProfile` usage except inside migration helper.

Update `page.tsx` if needed — e.g. pass FY default; no structural redesign.

Preserve existing UI: type-ahead, chips, browse checkboxes — behavior unchanged.

Do NOT add completeness gate UI here (Plan 01-03 handles import gate).
  </action>
  <verify>`npm run build` passes; `npm test` passes</verify>
  <done>Setup saves to SQLite; reload persists selections; localStorage migrated once then cleared</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm test` passes
- [ ] `npm run privacy:audit` passes (no new network/LLM deps)
- [ ] Manual: save setup → restart dev server → selections still present
</verification>

<success_criteria>

- Setup form reads/writes via Server Actions
- Existing localStorage profiles migrate automatically on first visit
- localStorage key cleared after successful migration
- No regression in Setup UI behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-persistence/01-02-SUMMARY.md` per summary template.
</output>
