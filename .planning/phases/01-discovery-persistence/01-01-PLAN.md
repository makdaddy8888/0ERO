---
phase: 01-discovery-persistence
plan: 01
type: execute
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - src/lib/discovery/discovery-repository.ts
  - drizzle/**
domain: next-js
---

<objective>
Add SQLite tables and a server-side discovery repository for institution/product selections.

Purpose: Replace localStorage as the source of truth while keeping all data in `./data/ledger.db`.
Output: `user_institutions` table, initial Drizzle migration, and `discovery-repository.ts` with load/save/isComplete.
</objective>

<execution_context>
./.cursor/get-shit-done/workflows/execute-plan.md
./.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@src/lib/db/schema.ts
@src/lib/db/index.ts
@src/lib/discovery/user-discovery.ts
@next.config.ts

**Tech stack available:** Next.js 15, Drizzle ORM, better-sqlite3 (already in `serverExternalPackages`)
**Established patterns:** Domain modules under `src/lib/`; money as cents; FY string `2025-26`
**Constraining decisions:**
- Privacy: no external API calls; SQLite file at `./data/ledger.db`
- better-sqlite3 is server-only — repository must NOT be imported from client components

**Issues being addressed:** SQLite unused; no Drizzle migrations committed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user_institutions table and generate migration</name>
  <files>src/lib/db/schema.ts, drizzle/**</files>
  <action>
Add a new `userInstitutions` table to `schema.ts`:

- `id` integer PK autoincrement
- `financialYear` text not null (e.g. `2025-26`)
- `kind` text not null — `'product'` or `'institution'`
- `entityId` text not null — product id (`nab-isaver`) or registry institution id
- `createdAt` text not null (ISO timestamp)

Add a unique composite index on `(financialYear, kind, entityId)` to prevent duplicates.

Extend `householdProfile` with optional `setupConfirmedAt` text column (ISO timestamp when user last saved setup).

Run `npm run db:generate` to produce the first migration under `drizzle/`. Commit the generated SQL + meta.

Do NOT add `discovery_sessions` or question-bank tables yet — Phase 2 scope.
Do NOT remove existing tables.
  </action>
  <verify>`npm run db:generate` succeeds; `drizzle/` contains new migration SQL; `npm run build` passes</verify>
  <done>Schema defines `userInstitutions`; migration files exist in repo; TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Wire getDb() migrations and discovery repository</name>
  <files>src/lib/db/index.ts, src/lib/discovery/discovery-repository.ts</files>
  <action>
Update `getDb()` in `src/lib/db/index.ts`:

- After opening SQLite, run Drizzle migrations from `./drizzle` using `migrate()` from `drizzle-orm/better-sqlite3/migrator`
- Ensure `data/` directory creation remains (existing behavior)

Create `src/lib/discovery/discovery-repository.ts` (server-only — no `"use client"`):

Export functions mirroring the current localStorage API shape:

```typescript
loadDiscoveryProfile(fy?: string): Promise&lt;UserDiscoveryProfile | null&gt;
saveDiscoveryProfile(profile: UserDiscoveryProfile): Promise&lt;void&gt;
isDiscoveryComplete(fy?: string): Promise&lt;boolean&gt;
```

Implementation:
- Read/write `userInstitutions` rows for the given FY
- Upsert `householdProfile` row for FY with `setupConfirmedAt` on save
- Reconstruct `UserDiscoveryProfile`: derive `confirmedInstitutionIds` via existing `deriveRegistryInstitutionIds()` from product ids + direct institution ids (import from `@/lib/institutions/products`)
- Default FY: `getDefaultFinancialYear()` from `user-discovery.ts`

Reuse `UserDiscoveryProfile` type from `user-discovery.ts` — do not duplicate.

Add file-level comment: "Server-only — import from Server Actions or Server Components only."

Do NOT call `getDb()` from any client component in this plan.
  </action>
  <verify>`npm run build` succeeds; grep confirms no client files import `discovery-repository.ts` yet</verify>
  <done>getDb runs migrations on first call; repository load/save/isComplete implemented and type-safe</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm test` passes (no regressions)
- [ ] `drizzle/` migration committed
- [ ] `discovery-repository.ts` is server-only (not imported by `"use client"` files)
</verification>

<success_criteria>

- All tasks completed
- `user_institutions` table exists with migration
- Server-side discovery repository can load/save/isComplete for a financial year
- No client-side SQLite usage introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-persistence/01-01-SUMMARY.md` per summary template.
</output>
